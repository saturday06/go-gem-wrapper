[package]
name = "example_rust"
version = "1.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
futures = "0.3"
magnus = "0.8"
tokio = { version = "1", features = ["full"] }

[profile.release]
lto = true
